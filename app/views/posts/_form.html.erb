<%= form_with(model: post, local: true, id: 'geolocate_me') do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, :class => 'text_input'%>
  </div>

  <div class='field'>
    <%= form.label :tag_list, 'Tags (separated by commas)' %><br/>
    <%= form.text_field :tag_list %>
  </div>
  
  <p>Images</p>
  <div id="uploads">
    <%= form.file_field :uploads, multiple: true %>
    <%= form.file_field :uploads, multiple: true %>
    <%= form.file_field :uploads, multiple: true %>
  </div>

  <div class="field">
    <%= form.label :date, :class => 'post_date_form' %>
    <%= form.datetime_select :date%>
  </div>

  <%= form.hidden_field  :latitude, :value => :latitude %>
  <%= form.hidden_field  :longitude, :value => :longitude %>

  <div class="actions">
   <button id="submit"> Submit</button>
  </div>
<% end %>
